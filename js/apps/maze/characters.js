/* ============================================================
   MAZE.EXE — characters  (PLACEHOLDER — not yet implemented)
   A single reusable Character class, instantiated and populated
   per character. Each character's dialogue lives here too, and is
   presented through the dialogue-box system (see state.js).
   ============================================================ */

// export class Character {
//   constructor(def = {}){
//     // TODO: id, name, portrait, dialogue tree, flags/relationship state.
//   }
//   // TODO: say() / advance() — drive a dialogue box from the tree.
// }

// Concrete character definitions + their dialogue will be declared below.
